{
  "triggers": [
    {
      // name: 触发器的名字，规则见下方说明
      "name": "out_date_url",
      // type: 触发器类型，目前仅支持 timer (即 定时触发器)
      "type": "timer",
      // config: 触发器配置，在定时触发器下，config 格式为 cron 表达式，规则见下方说明
      "config": "* */30 * * * * *"
    }
  ]
}